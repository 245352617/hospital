namespace YiJian.MasterData.Labs.Position
{
    using System;
    using System.Collections.Generic;
    using System.Threading.Tasks;
    using Shouldly;
    using Xunit;

    /// <summary>
    /// 检验标本采集部位 领域实体测试
    /// </summary>
    public class LabSpecimenPositionTests : MasterDataDomainTestBase
    {
        public LabSpecimenPositionTests()
        {  
        }

        [Fact]
        public void ModifyTest()
        {  
            /*
            // Arrange 为测试做准备工作

            // 标本编码
            string specimenCode = "";
            // 标本名称
            string specimenName = "";
            // 采集部位编码
            string positionCode = "";
            // 采集部位名称
            string positionName = "";
            // 排序号
            int indexNo = 0;
            // 拼音码
            string pyCode = "";
            // 
            bool isActive = true;

            var labSpecimenPosition = new LabSpecimenPosition(specimenCode,// 标本编码
                specimenName,   // 标本名称
                positionCode,   // 采集部位编码
                positionName,   // 采集部位名称
                indexNo,        // 排序号
                pyCode,         // 拼音码
                isActive
                );

            // Act 运行实际测试的代码

            labSpecimenPosition.Modify(specimenName,// 标本名称
                positionCode,   // 采集部位编码
                positionName,   // 采集部位名称
                indexNo,        // 排序号
                pyCode,         // 拼音码
                isActive
                );

            // Assert 断言，检验结果

            labSpecimenPosition.SpecimenCode.ShouldNotBeNull();
            */            
        }
    }
}
