namespace YiJian.MasterData.Labs.Container
{
    using System;
    using System.Collections.Generic;
    using System.Threading.Tasks;
    using Shouldly;
    using Xunit;

    /// <summary>
    /// 容器编码 领域实体测试
    /// </summary>
    public class LabContainerTests : MasterDataDomainTestBase
    {
        public LabContainerTests()
        {  
        }

        [Fact]
        public void ModifyTest()
        {  
            /*
            // Arrange 为测试做准备工作

            // 容器编码
            string containerCode = "";
            // 容器名称
            string containerName = "";
            // 容器颜色
            string containerColor = "";
            // 
            bool isActive = true;

            var labContainer = new LabContainer(containerCode,// 容器编码
                containerName,  // 容器名称
                containerColor, // 容器颜色
                isActive
                );

            // Act 运行实际测试的代码

            labContainer.Modify(containerName,// 容器名称
                containerColor, // 容器颜色
                isActive
                );

            // Assert 断言，检验结果

            labContainer.ContainerCode.ShouldNotBeNull();
            */            
        }
    }
}
